#include "Base.xcconfig"

GCC_PREPROCESSOR_DEFINITIONS = $(inherit) DEBUG
HEADER_SEARCH_PATHS = "$(BUILT_PRODUCTS_DIR)/usr/local/include"
BUNDLE_ID_SUFFIX = .debug
BUNDLE_NAME_SUFFIX = .D
COPY_PHASE_STRIP = NO
GCC_OPTIMIZATION_LEVEL = 0
